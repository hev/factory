# Worker evaluation rubric

- **Poor:** a user-visible step on an instance with `preview_domains` has no deployed-preview evidence, unless the ten-minute no-preview fallback includes a substantiated one-line stand-in; a failed deployment alone does not make the session poor.
