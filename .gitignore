# Release tarballs, built by .github/build-release.sh.
dist/

# Session transcripts the local tooling drops in a checkout.
.claude/traces/
.factory-watermark

__pycache__/
*.pyc

# Machine-local files from the role-era factory (instance configs, identity
# and notify drop-ins, the picker build). They name real hosts and hold
# credential hooks; they stay on the machine that has them and never here.
bin/
factories/
identity/
notify/
